type: string
enum:
  - InSession
  - OffSession
description: >-
  Indicates interaction type, currently if customer is present or not present.
  If not provided the default is `'OffSession'` (customer is not present) when
  the individual VRP payment is made. 
