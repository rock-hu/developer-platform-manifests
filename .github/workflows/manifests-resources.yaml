name: manifests-resources
on:
  push:
    branches:
      - "master"
    paths:
      - ".github/workflows/manifests-resources.yaml"
      - "manifests/resources/**"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      # - run: npm install --force --verbose
      - run: npm install -g @roadiehq/backstage-entity-validator
      - run: validate-entity manifests/resources.yaml