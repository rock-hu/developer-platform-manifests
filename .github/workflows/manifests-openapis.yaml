name: manifests-openapis
on:
  push:
    branches:
      - "master"
    paths:
      - ".github/workflows/manifests-openapis.yaml"
      - "manifests/openapis/**"
jobs:
  validate-entity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      # - run: npm install --force --verbose
      - run: npm install -g @roadiehq/backstage-entity-validator
      - run: validate-entity manifests/openapis.yaml